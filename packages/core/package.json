{
  "name": "@prism/core",
  "version": "0.1.0",
  "private": true,
  "description": "Prism indexing engine, database, and domain types",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "pino": "^9.6.0"
  },
  "engines": {
    "node": ">=22"
  }
}
