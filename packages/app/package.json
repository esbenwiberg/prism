{
  "name": "@prism/app",
  "version": "0.1.0",
  "private": true,
  "description": "Prism CLI, dashboard, and blueprint generator",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@prism/core": "*"
  },
  "engines": {
    "node": ">=22"
  }
}
